services:
  - type: web
    name: division-estimate-suite
    runtime: docker
    plan: starter
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SEED_DEMO
        value: "false"
      - key: NODE_ENV
        value: production
